Приложение на Flask для в заимодействия с БД



Приложение позволит вести учет товаров их актуальную цену и статус,
а так же кто из продавцов продает и какой статус имеет
и собственно сами продажи

* схема не идеальная можем обсудить



как должноо все работать:
  В WEB приложении есть формы для ввода информации в БД
  так же данные возможность запускать процедуры
  выводить массивы для просмотра на веб страницу
  и самое главное (загружать, обрабатывать и хранить на сервере файлы) - в схеме этого нет, не знаю как лучше делать,
  хранить доки в базе или просто на сервере, в папках а в базе ссылки
  
  
  что не понимаю сейчас / как будет работать python при запуске функций если работают сразу несколько человек (например при обработке файлов) это обсудим позже
